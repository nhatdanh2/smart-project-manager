{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "smart-pm.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "smart-pm.labels" . | nindent 4 }}
{{- end }}
